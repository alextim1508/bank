spring:
  application:
    name: bank-notification-service

  datasource:
    url: jdbc:postgresql://localhost:5439/notification-db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/master.yml
    url: jdbc:postgresql://localhost:5439/notification-db
    user: postgres
    password: postgres

sso:
  client-registration-id: bank

