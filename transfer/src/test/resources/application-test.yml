spring:
  application:
    name: bank-transfer-service
  datasource:
    url: jdbc:postgresql://localhost:5436/transfer-db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/master.yml
    url: jdbc:postgresql://localhost:5436/transfer-db
    user: postgres
    password: postgres

sso:
  client-registration-id: bank